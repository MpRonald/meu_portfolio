name: Keep Render Awake

on:
  schedule:
    - cron: "*/5 * * * *"   # a cada 5 minutos
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render healthz
        run: |
          curl -fsS https://ronaldpio.onrender.com/healthz > /dev/null
